import { SelectedOptionStoryTestDirective } from './selected-option-story-test.directive';

describe('SelectedOptionStoryTestDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedOptionStoryTestDirective();
    expect(directive).toBeTruthy();
  });
});
