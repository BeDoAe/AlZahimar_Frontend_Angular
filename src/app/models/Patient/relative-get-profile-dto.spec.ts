import { RelativeGetProfileDTO } from './relative-get-profile-dto';

describe('RelativeGetProfileDTO', () => {
  it('should create an instance', () => {
    expect(new RelativeGetProfileDTO()).toBeTruthy();
  });
});
