import { AvailableSlotsDTO } from './available-slots-dto';

describe('AvailableSlotsDTO', () => {
  it('should create an instance', () => {
    expect(new AvailableSlotsDTO()).toBeTruthy();
  });
});
